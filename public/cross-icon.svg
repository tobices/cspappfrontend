<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 200 200" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
  <!-- CHURCH -->
  <text x="100" y="40" text-anchor="middle" font-family="Georgia, serif" font-size="24" font-weight="bold" fill="currentColor" stroke="none">CHURCH</text>

  <!-- OF -->
  <text x="100" y="75" text-anchor="middle" font-family="Georgia, serif" font-size="18" fill="currentColor" stroke="none">OF</text>

  <!-- CE -->
  <text x="100" y="120" text-anchor="middle" font-family="Georgia, serif" font-size="32" font-weight="bold" fill="currentColor" stroke="none">CE</text>

  <!-- ESTE -->
  <text x="100" y="160" text-anchor="middle" font-family="Georgia, serif" font-size="24" fill="currentColor" stroke="none">ESTE</text>
</svg>